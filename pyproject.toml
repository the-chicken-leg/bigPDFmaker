[project]
name = "bigpdfmaker"
version = "0.1.0"
description = "Combine PDF documents from a folder (non-recursively) into a big PDF (sorted by filename)."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pypdf[full]>=6.9.2",
]
